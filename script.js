function drum(e){
if (e.propertyName !== "transform") return;
e.target.classlist.remove(playing);

};

function music(e){
    let soundss {audio[data-key="${e.keyCode}"]
    if (!audio) return;
    
};




  let keys{div[data-key="${e.keyCode}" class="playing"]

};
    playing{
        currentTime=0;
    }
